<!DOCTYPE html>
<html lang="es">
<!-- Include Head -->

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<link width="" rel="shortcut icon" href="./images/logo-camisetasFC.png" />

	<title>CamisetasFC</title>
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.11.2/css/all.min.css">
	<!-- Bootstrap Grid Stylesheet -->
	<link rel="stylesheet" href="/stylesheets/bootstrap-grid.min.css">
	<!-- Own Stylesheet -->
	<link rel="stylesheet" href="/stylesheets/app.css"></head>

<body>
	<!-- Header -->
	<header class="main-header">
		<% if (user != null) {%>
			<%- include('./partials/headerLogueado.ejs')%>
		<%} else{%>
			<%- include('./partials/header.ejs')%>
		<%}%>
	</header>
	<!-- /Header -->

	<!-- Novedades -->
	<%if(usuarios.length == 0){%>
		<h1 display="flex" align="center" class="products-title">No hay resultados de búsqueda para "<%=busqueda%>"</h1>
	<%}else{%>
		<h1 display="flex" align="center" class="products-title">Resultados de búsqueda para "<%=busqueda%>"</h1>
		<div class="container products-wrapper">
			<div class="row">
				<div class="col-12">
				</div>
					<% for (let i = 0; i < usuarios.length; i++) { %>
					<div class="col-12 col-sm-6 col-lg-3">
					<section  class="product-box">
						<a href="/users/profile/<%= usuarios[i].id %>">
						<figure class="product-box_image">
							<img src="<%= usuarios[i].foto_de_perfil %>"  alt='' height="250px" width="330px">
						</figure>
						<article class="product-box_data">
							<h2><%= usuarios[i].username %></h2>
                            <p><%= usuarios[i].email %></p>
						</article>
						</a>
					</section>
					</div>
	  				<% } %>
	<%}%>
	<!-- footer -->
	<footer class="main-footer">
		<%- include('./partials/footer.ejs')%>
	</footer>
	<!-- /footer -->
</body>

</html>